# Algoritmit ja tekoäly projekti
## Aineopintojen harjoitustyö - Gomoku

### instrc (WIP)
` Poetry install `

`eval $(poetry env activate)`

`cd C:\path\where\the\files\were\saved
pyinstaller example.py pisqpipe.py --name pbrain-BeefGomoku.exe --onefile`

Gomocup Manager, select .exe
